diff --git a/node_modules/@agentmail/convex/dist/component/convex.config.d.ts b/node_modules/@agentmail/convex/dist/component/convex.config.d.ts
index 08c43c3..f28e269 100644
--- a/node_modules/@agentmail/convex/dist/component/convex.config.d.ts
+++ b/node_modules/@agentmail/convex/dist/component/convex.config.d.ts
@@ -1,3 +1,3 @@
-declare const component: import("convex/server").ComponentDefinition<any>;
+declare const component: import("convex/server").ComponentDefinition<any, any>;
 export default component;
 //# sourceMappingURL=convex.config.d.ts.map
\ No newline at end of file
diff --git a/node_modules/@agentmail/convex/dist/component/convex.config.js b/node_modules/@agentmail/convex/dist/component/convex.config.js
index 3d15ed6..dabe078 100644
--- a/node_modules/@agentmail/convex/dist/component/convex.config.js
+++ b/node_modules/@agentmail/convex/dist/component/convex.config.js
@@ -1,6 +1,12 @@
 import { defineComponent } from "convex/server";
+import { v } from "convex/values";
 import workpool from "@convex-dev/workpool/convex.config";
-const component = defineComponent("agentmail");
+const component = defineComponent("agentmail", {
+    env: {
+        AGENTMAIL_API_KEY: v.string(),
+        AGENTMAIL_BASE_URL: v.optional(v.string()),
+    },
+});
 component.use(workpool, { name: "sendPool" });
 component.use(workpool, { name: "callbackPool" });
 export default component;
diff --git a/node_modules/@agentmail/convex/dist/component/lib.js b/node_modules/@agentmail/convex/dist/component/lib.js
index c245802..3d8bfa7 100644
--- a/node_modules/@agentmail/convex/dist/component/lib.js
+++ b/node_modules/@agentmail/convex/dist/component/lib.js
@@ -200,11 +200,21 @@ export const performSend = internalAction({
         });
         if (!row)
             return null;
+    // Never reuse a provider idempotency key after its 24-hour retention window.
+    if (Date.now() - row._creationTime >= 23 * 60 * 60 * 1000) {
+      await ctx.runMutation(internal.lib.markSendFailed, {
+        outboundId: args.outboundId,
+        errorMessage: "Idempotency window expired; delivery requires reconciliation",
+      });
+      return null;
+    }
+
         const path = sendPath(row.inboxId, row.kind, row.parentMessageId);
         try {
             const response = (await agentmailFetch(path, {
                 method: "POST",
                 body: row.payload,
+        idempotencyKey: `might-${args.outboundId}`,
             }));
             // Defensive: AgentMail's send endpoints always return JSON; guard the
             // pathological case so we surface a clear error rather than NPE on
diff --git a/node_modules/@agentmail/convex/dist/component/utils.d.ts b/node_modules/@agentmail/convex/dist/component/utils.d.ts
index bbcdc4c..8556327 100644
--- a/node_modules/@agentmail/convex/dist/component/utils.d.ts
+++ b/node_modules/@agentmail/convex/dist/component/utils.d.ts
@@ -14,6 +14,7 @@ export declare class AgentMailApiError extends Error {
 type FetchOptions = {
     method: "GET" | "POST" | "PATCH" | "DELETE";
     body?: unknown;
+    idempotencyKey?: string;
     query?: Record<string, string | number | boolean | undefined | null>;
 };
 export declare function agentmailFetch(path: string, opts: FetchOptions): Promise<unknown>;
diff --git a/node_modules/@agentmail/convex/dist/component/utils.js b/node_modules/@agentmail/convex/dist/component/utils.js
index 47ab5db..1ed5e85 100644
--- a/node_modules/@agentmail/convex/dist/component/utils.js
+++ b/node_modules/@agentmail/convex/dist/component/utils.js
@@ -45,6 +45,7 @@ export async function agentmailFetch(path, opts) {
     const headers = {
         Authorization: `Bearer ${apiKey}`,
     };
+    if (opts.idempotencyKey) headers["Idempotency-Key"] = opts.idempotencyKey;
     let body;
     if (opts.body !== undefined) {
         headers["Content-Type"] = "application/json";
diff --git a/node_modules/@agentmail/convex/src/component/convex.config.ts b/node_modules/@agentmail/convex/src/component/convex.config.ts
index b1cc7f1..8117bd7 100644
--- a/node_modules/@agentmail/convex/src/component/convex.config.ts
+++ b/node_modules/@agentmail/convex/src/component/convex.config.ts
@@ -1,7 +1,13 @@
 import { defineComponent } from "convex/server";
+import { v } from "convex/values";
 import workpool from "@convex-dev/workpool/convex.config";

-const component = defineComponent("agentmail");
+const component = defineComponent("agentmail", {
+  env: {
+    AGENTMAIL_API_KEY: v.string(),
+    AGENTMAIL_BASE_URL: v.optional(v.string()),
+  },
+});
 component.use(workpool, { name: "sendPool" });
 component.use(workpool, { name: "callbackPool" });

diff --git a/node_modules/@agentmail/convex/src/component/lib.ts b/node_modules/@agentmail/convex/src/component/lib.ts
index 121dd58..197617d 100644
--- a/node_modules/@agentmail/convex/src/component/lib.ts
+++ b/node_modules/@agentmail/convex/src/component/lib.ts
@@ -249,12 +249,22 @@ export const performSend = internalAction({
     });
     if (!row) return null;

+    // Never reuse a provider idempotency key after its 24-hour retention window.
+    if (Date.now() - row._creationTime >= 23 * 60 * 60 * 1000) {
+      await ctx.runMutation(internal.lib.markSendFailed, {
+        outboundId: args.outboundId,
+        errorMessage: "Idempotency window expired; delivery requires reconciliation",
+      });
+      return null;
+    }
+
     const path = sendPath(row.inboxId, row.kind, row.parentMessageId);

     try {
       const response = (await agentmailFetch(path, {
         method: "POST",
         body: row.payload,
+        idempotencyKey: `might-${args.outboundId}`,
       })) as { message_id: string; thread_id: string } | null;

       // Defensive: AgentMail's send endpoints always return JSON; guard the
diff --git a/node_modules/@agentmail/convex/src/component/utils.ts b/node_modules/@agentmail/convex/src/component/utils.ts
index e20117d..6dead44 100644
--- a/node_modules/@agentmail/convex/src/component/utils.ts
+++ b/node_modules/@agentmail/convex/src/component/utils.ts
@@ -33,6 +33,7 @@ export class AgentMailApiError extends Error {
 type FetchOptions = {
   method: "GET" | "POST" | "PATCH" | "DELETE";
   body?: unknown;
+  idempotencyKey?: string;
   query?: Record<string, string | number | boolean | undefined | null>;
 };

@@ -62,6 +63,7 @@ export async function agentmailFetch(
   const headers: Record<string, string> = {
     Authorization: `Bearer ${apiKey}`,
   };
+  if (opts.idempotencyKey) headers["Idempotency-Key"] = opts.idempotencyKey;
   let body: string | undefined;
   if (opts.body !== undefined) {
     headers["Content-Type"] = "application/json";
