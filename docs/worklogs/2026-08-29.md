# Might worklog — 2026-08-29

## Session 0 — Production Companion Manifestation checkpoint

- **Authorization:** the user explicitly approved setting the existing OpenAI key on the Convex production deployment and deploying the verified Manifestation/daylight slice. This approval did not authorize email outreach, social posting, final submission, destructive cleanup, or any additional production mutation.
- **Production environment:** copied `OPENAI_API_KEY` from the project dev default into production through a non-output pipeline. The key value was not printed, copied into source, stored in Git, or included in this log.
- **Backend deploy:** `npx convex deploy --yes --typecheck enable --message "Manifestation and daylight UI foundation"` deployed the schema, functions, and two `companionManifestations` indexes to `hushed-stork-401`. No existing index was deleted.
- **Static deploy:** `npx @convex-dev/static-hosting deploy --skip-build --skip-convex` published the already verified build as deployment `bd361568-307c-4465-983d-7ab3c8be110e`. Static Hosting replaced its old hashed asset manifest; it did not delete application data.
- **Deploy wrapper note:** `npm run deploy` successfully built the app but stopped before backend deployment because its wrapper could not prompt in the non-interactive terminal. It made no partial backend deployment. The already approved deploy was completed with the two explicit commands above.
- **Public checks:** `https://hushed-stork-401.convex.site/` and `/talk` returned the SPA, `/api/health` returned `{"status":"live","service":"might-api"}`, and the deployed JavaScript referenced `https://hushed-stork-401.convex.cloud`.
- **Real production E2E:** a fresh browser-scoped anonymous session submitted `A tiny dawn archivist with an apricot scarf, lavender light, and a brave but gentle smile.` against the production backend. Realtime state advanced `generating_brief → generating_image → ready`; the result rendered as an original Webtoon-style companion with apricot scarf, lavender light, archival details, and no visible famous-character logo or signature costume.
- **Persistence and handoff:** after a full reload, the same Convex-stored image and adaptation note returned without another provider call. Selecting `Start talking` carried the compact generated companion into Talk while the sidebar remained `Convex live`.
- **Runtime evidence:** production logs recorded `manifestation:beginGeneration`, `manifestation:markBriefGenerated`, `manifestation:completeGeneration`, `manifestationOpenai:generateAssets`, and `manifestation:generate`. The provider action completed successfully in 61,224 ms and the public action in 61,279 ms.
- **External effects:** executed one successful production OpenAI structured-response call and one successful production image-generation call; stored one generated PNG and its traceable manifestation record in production Convex. No email, Firecrawl call, social post, or final submission occurred.
- **Git/public boundary:** `main` at `75d8097` was already pushed to the public repository `https://github.com/Ranopha/might` before deployment. The source deployed to production matches that commit; only evidence logs are being added afterward.
- **Privacy:** the verification used a synthetic prompt and a new browser-scoped anonymous session. Session keys, provider credentials, storage URLs, and external request identifiers are not recorded here.
- **Rollback:** the Git slice can be reverted and redeployed. The production environment variable, manifestation row, and generated Storage blob are external state and require a separate explicit, targeted cleanup authorization; none was performed.
- **Maturity:** Companion Manifestation is now **public deployment verified**. It is not yet a complete sponsor E2E. Firecrawl, AgentMail, living-memory extraction, contextual matching, consent-bound outbound, verified inbound reply, and Connected remain open gates.
- **Next tracer bullet:** implement a real OpenAI Talk reply plus source-linked private memory candidates in Convex, then prove the Me surface updates through a realtime query.

## Session 1 — Talk → OpenAI reply → Living Memory

- **Authorization:** the user asked to keep advancing the hackathon build. Repository implementation, dependency installation, development Convex resources, commit, and push are covered by the standing project authorization. No fresh production-deploy approval was given, so production was not changed. No outreach, social post, or final submission occurred.
- **Scope decision:** this session implemented only the next ordered tracer bullet: save a natural conversation turn, obtain a real OpenAI reply, extract useful source-linked private memories, and show them in Me through Convex realtime. It did not begin Firecrawl, matching, consent outreach, or AgentMail.
- **TDD sequence:** behavior tests first proved the absence of the memory module, idempotent client turn, asynchronous processing state, turn-aware idempotent commit, and session-owned confirm/edit/forget controls. The finished suite contains eight passing tests across Talk/Memory and Manifestation.
- **Convex Agent:** mounted `@convex-dev/agent` beside Static Hosting. Each primary conversation lazily creates one Agent component thread, and user messages are saved through that thread while the app keeps its own bounded, traceable message and turn records.
- **Async OpenAI work:** `appendUserMessage` persists once and schedules `talkOpenai:generateTurn`. The Node action asks OpenAI for a concise contextual response, then performs a second strict structured extraction pass. Failed provider work settles the turn into an explicit failure state instead of leaving the UI indefinitely processing.
- **Memory policy:** extraction accepts at most four statements about the current user, rejects filler, third-party facts, sensitive content, low-confidence candidates, and exact duplicates, and preserves source message, semantic type, explicitness, confidence, privacy, model, response trace, and timestamps. Public memory queries and controls require ownership of the browser-scoped session. Memory remains private by default.
- **UI behavior:** Talk shows the persisted user message immediately, an animated thinking state, the later Agent reply, and an honest privacy disclosure. Me shows low-density editorial cards with live count, source/private labels, and `That's right`, `Not quite`, and `Forget` controls. Corrected memories are marked as user-corrected and confirmed; reduced-motion behavior remains supported.
- **Development runtime:** `npx convex codegen` and `npx convex dev --once` loaded the final functions and Agent component into personal development deployment `vibrant-wren-913`. No production deployment or production data mutation occurred.
- **Real dev E2E:** a fresh synthetic user message stated that Alex manages a convenience store in Taoyuan, has around ten years of woodworking experience, owns the tools, and still builds on weekends. A real OpenAI response acknowledged that context and asked one natural follow-up question. Four private memories appeared in Me without refresh.
- **Memory controls and persistence:** confirmed the tools/weekend memory, corrected the woodworking-experience memory, and forgot the synthetic name. The live count changed from four to three. A full reload preserved all three remaining memories, their confirmation/correction state, and the Talk reply.
- **Runtime evidence:** the Convex action log recorded user-turn persistence, context lookup, Agent thread/message calls, memory commit, confirm, edit, and forget. The real `talkOpenai:generateTurn` action completed successfully in about 9.6 seconds. Browser inspection after the final dev sync showed no warning or error.
- **Verification:** `npm test` passed 8/8 tests; `npm run lint`, `npm run typecheck`, `npm run build`, `npx convex codegen`, and final `npx convex dev --once` passed. The production build contains 510 transformed modules and emitted a 446.96 kB JavaScript bundle before gzip.
- **External effects:** performed one real OpenAI conversation response and one real OpenAI structured memory-extraction response against synthetic dev data. No Firecrawl request, email, inbound webhook, social action, or submission occurred.
- **Review:** checked all Convex exports for validators, scheduler targets for internal visibility, query bounds/index use, session ownership, idempotency, and deterministic mutation behavior. The UI does not receive the component thread ID. Forgotten memories cannot be reconfirmed or edited. The accepted hackathon boundary is that the high-entropy browser session key remains bearer identity until a later auth decision; it is not shared across fresh browsers.
- **Rollback:** revert this slice and run the development push again. The synthetic dev conversation, Agent component messages, turns, and memories are external development data; deleting them would require a separate targeted cleanup decision. Production needs no rollback because it was untouched.
- **Maturity:** Talk → OpenAI reply → Living Memory → Me realtime is **real integration verified on development**. It is not public-deployment verified and does not complete the hackathon E2E.
- **Next tracer bullet:** after a fresh production-deploy approval for this verified slice, implement the Firecrawl World Sensor and preserve the public URL and Firecrawl receipt needed for contextual match evidence.
