# Might worklog — 2026-08-31

## Fixed-room Talk opening

### Authorization and design decisions

- The owner selected the bright `Sunlit Paper Alcove` direction and the product idea `固定房間，世界來敲門` for the Talk opening.
- The approved original paper-and-leaf orb remains Might's default identity. Its name remains user-changeable later; no naming feature was added in this slice.
- The room background and primary/hover/pressed/secondary button plates were approved. The foreground frame was accepted provisionally, so the implementation keeps it at low opacity and does not let it cover the conversation.
- Scope stayed on the opening hero and its direct responsive states. Existing Talk, living memory, Firecrawl, Match, consent, AgentMail, reply, and Connected behavior were not changed.

### Product and visual implementation

- Replaced the old generated CSS orb artwork with the approved transparent companion asset while preserving the existing `Orb` and `CompanionPresence` seams.
- Added a layered room scene: full-room background, softly breathing companion, low-opacity foreground depth, and a warm paper conversation page.
- Used the approved image plates for primary idle, hover, and pressed states and for the secondary decision. Button labels remain accessible live text rather than being baked into the images.
- On desktop the room is a single complete hero. At 390 px, the full room remains an establishing image and the conversation flows below it so the alcove and outside door are not lost to a crop.
- Reduced-motion handling stops the room and foreground loops; Motion's reduced-motion preference also makes the companion static.

### Verification and evidence

- TypeScript typecheck passed.
- ESLint passed.
- Vitest passed 5 files / 24 tests.
- Vite production build passed.
- In-app browser checks covered 1440 × 1000 desktop and 390 × 844 mobile layouts, `Shape my form`, `Keep this form`, the manifestation input state, and entry into the existing private chat.
- The primary button loaded distinct idle and hover image assets. The browser console returned no warning or error.
- The approved source and desktop implementation were placed in one side-by-side comparison and reviewed. `design-qa.md` records P0, P1, and P2 as passed.

### External effects, maturity, and rollback

- No Convex schema, function, component, environment value, database row, OpenAI call, Firecrawl call, AgentMail message, webhook, or production deployment changed in this session.
- Maturity is **本機已驗證** for the new Talk opening. It is not yet **公開部署已驗證**.
- Rollback is the previous frontend revision plus removal of the new public assets. The existing backend evidence chain is unaffected.
- The foreground frame remains intentionally provisional; if direct browser review finds it distracting, it can be removed without changing layout or behavior.

### Session close

- Ran the `/hackathon` workflow after evidence review. The public log records only the locally verified UI slice and does not claim a production release or new Sponsor integration work.

## Fixed-room surface asset family

### Authorization and design decisions

- The owner asked Me, Might Found, and Connections to remain in the same visual language as the approved Talk room and authorized the remaining asset work.
- All four surfaces are treated as different story moments in one fixed room: living memory rests in the alcove, the outside world knocks through the doorway, and a real two-way hello spans the room.
- This slice intentionally produced assets and motion direction only. It did not replace the existing Me, Might Found, or Connections behavior yet.

### Asset delivery

- Added a transparent botanical memory folio for Me, a paper-leaf messenger for Might Found, and two connected lantern-pods for Connections under `public/assets/surfaces/`.
- The three assets use the approved ivory, sage, amber, handmade-paper, fine-ink, non-photorealistic Webtoon language. They contain no text, dashboard iconography, logo, famous IP, or pre-rendered controls.
- Recorded the final built-in image-generation prompt set, intended room placement, and reduced-motion-aware motion direction in `docs/design/fixed-room-surface-assets-v1.md`.
- The generator's initial checkerboard-looking results were RGB, so they were rejected as fake transparency. Clean keyed generations were converted into deterministic alpha mattes and composited over the approved room before acceptance.

### Verification, external effects, and maturity

- All three final PNGs are RGBA with real 0–255 alpha ranges. Same-room composites confirmed the intended desktop scale, reading order, and absence of visible key-color spill.
- No application source, Convex schema/function/environment/data, Sponsor API, email, webhook, public deployment, or production state changed in this slice. Codex built-in image generation was used instead of the product's OpenAI API.
- Maturity is **asset locally reviewed**. Integration into the three product surfaces, responsive browser QA, motion implementation, and public deployment remain separate work.
- Ran the `/hackathon` workflow after the asset and evidence review; the public log records an asset-only working-tree checkpoint without claiming deployment.

## Fixed-room surface integration and companion identity

### Authorization and decisions

- The owner authorized integration of the Me, Might Found, and Connections assets and confirmed that people should eventually create both the image and name of their own Mighty.
- The approved paper-and-leaf orb named `Might` remains the complete house default. Custom identity stays inside the existing Shape flow instead of adding a settings dashboard or fifth primary surface.
- A future user-owned OpenAI-key guide was specified but not implemented. Requiring a secret before first value would conflict with the anonymous consumer flow, and the current app has no authenticated secret vault. The hackathon path therefore keeps the limited server-side Sponsor key while BYOK remains a later, explicit advanced option.

### Implementation

- Added one shared `SurfaceRoomHero` that composes the approved room, foreground, companion, and transparent story asset for Me, Found, and Connections. Existing Convex queries, mutations, Sponsor actions, consent controls, and connection state machine were not replaced.
- Me's memory folio uses a slow page-light breath. Found keeps the current companion in the alcove while the doorway messenger becomes more present during sensing. Connections progresses from faint idle pods through Replied to a warmer Connected pose. Reduced-motion mode renders settled assets and stops room loops.
- Mobile uses a full-room establishing panel followed by the live story copy on warm paper. Desktop keeps title and illustration together on the room wall without creating a dashboard card.
- Added an optional, validated `name` to companion manifestations. Old records fall back to `Might`; the request id now binds both name and description; Talk uses the persisted name in headings, speaker labels, accessible descriptions, composer copy, and generated-form confirmation.
- Recorded the safe user-owned-key experience and minimum authenticated vault boundary in `docs/design/companion-identity-and-byok-boundary-v1.md`. No decorative key input or secret storage was added.

### Verification and QA

- A targeted manifestation test first failed on the new `name` field, then passed after the schema/action/view implementation. The full suite passed 5 files / 24 tests.
- ESLint, TypeScript typecheck, Vite production build, Convex codegen/development sync, and `git diff --check` passed. A lint finding for a control-character regular expression was replaced with explicit code-point validation and rechecked.
- The three PNGs retain real alpha ranges, contain transparent pixels, and have zero remaining saturated key-green pixels under the technical edge check.
- In-app browser QA covered 1440 × 1000 desktop and 390 × 844 mobile views for Me, Found, Connections, and the Shape form. All tested routes matched viewport width with no horizontal overflow. The first-paint asset fade and overly faint idle messenger were fixed during review.
- `design-qa.md` records the source truth, desktop/mobile captures, combined comparison, findings, and final `passed` result.

### External effects, maturity, and rollback

- Convex codegen synced the backward-compatible optional `name` field and updated manifestation functions to development. No production Convex deploy or static-hosting release occurred.
- Visual QA did not call OpenAI, Firecrawl, or AgentMail; no application row, email, webhook, approval, or external contact was created.
- Maturity is **本機已驗證** for the room integration and **development-synced** for name persistence. The BYOK concept is **規格** only. Public-production behavior is unchanged and not claimed.
- Rollback is the prior surface components/CSS plus removal of the optional name wiring. Existing manifestations remain readable because the new schema field is optional; the Sponsor E2E state machine is unaffected.

### Session close

- Ran the `/hackathon` workflow after verification. The public log replaces the earlier asset-only working-tree checkpoint with the integrated, locally verified slice and explicitly preserves the no-production/no-BYOK boundary.

## Owner-directed surface composition correction

### Authorization and visual decisions

- The owner supplied direct browser screenshots and asked for three bounded corrections: move Found toward the right-hand door, make Me slightly smaller and seat it on the alcove platform, and make the Connections illustration slightly less transparent.
- The fixed-room art direction, four-surface information architecture, live product behavior, and existing generated assets remain unchanged. This was a composition and resting-opacity correction only.

### Implementation and comparison

- Found now tells the door-opening story spatially: Might occupies the right doorway, the paper messenger sits higher beside the door, and the headline uses the vacated left alcove. The same narrative direction is retained on mobile.
- Me's memory folio is narrower and higher at both medium and mobile breakpoints so its wooden base rests on the same built-in platform that supports the Talk orb.
- Connections' idle illustration opacity increased from `0.50` to `0.64`; the reduced-motion resting value increased from `0.58` to `0.66`. Its eventual-human connection metaphor remains translucent rather than becoming a solid decorative object.
- Compared the three supplied 2940 × 1912 screenshots against live 960 × 1000 revisions, then separately checked 390 × 844 mobile captures. All tested routes matched viewport width without horizontal overflow.
- Saved the source-to-revision review at `/Users/liuenyan/.codex/visualizations/2026/08/31/might-surface-feedback-round2/user-feedback-vs-revised.png`; `design-qa.md` records the P1/P2 findings and final passed result.

### Verification

- `git diff --check`, ESLint, TypeScript typecheck, all 24 behavior tests, and the Vite production build passed after the correction.
- The visual checks covered Me, Found, and Connections at 1024 × 900, 960 × 1000, and 390 × 844. The final owner-comparison review used the 960 and 390 captures because they reproduce the supplied medium layout and the narrow mobile breakpoint.

### External effects, maturity, and rollback

- No Convex schema, function, environment, data, OpenAI call, Firecrawl call, AgentMail message, webhook, approval, or public deployment changed in this correction.
- Maturity remains **本機已驗證** for the corrected composition. Production behavior is unchanged and is not claimed.
- Rollback is limited to the revised room-placement CSS and two Connections opacity constants; the vertical-slice state machine and Sponsor evidence are unaffected.

## Production promotion of the fixed-room slice

### Authorization and guarded preflight

- The owner explicitly authorized `main@3b053a4` for both the Convex backend and static frontend on production `hushed-stork-401`.
- A production dry-run found 17 additive Consent/AgentMail/Connected indexes and no index deletion. It also proved that a frontend-only upload would be unsafe because the released Found and Connections screens depend on 12 functions not yet present in production.
- The first backend finalization failed closed because `AGENTMAIL_API_KEY` was actually absent. A previous single-name presence probe had produced a false positive; the corrected name-only environment listing showed that production contained only the OpenAI and Firecrawl variables. Function inventory remained at 40, both public routes stayed `200`, and the failed attempt did not activate the new code.
- After a second exact authorization, copied only `AGENTMAIL_API_KEY`, `AGENTMAIL_INBOX_ID`, and `AGENTMAIL_ALLOWED_RECIPIENTS` from development to production without displaying their values. `AGENTMAIL_WEBHOOK_SECRET` was not copied, no webhook was registered, and no email was sent.

### Deployment and verification

- `npx convex deploy --yes` installed the AgentMail component and its callback/send pools, added the approved schema/functions, and reported no index deletion.
- Production function inventory increased from 40 to 52. `connections:latest`, `sendApprovedPitch`, `confirmConnected`, and the internal inbound handler were present after deployment; `/api/health` returned `200`.
- Published 13 static files as deployment `bb26da34-811a-461a-8abb-c316daff83b5` using a production-targeted build and a separate static upload, avoiding a duplicate backend deploy.
- `/`, `/talk`, `/me`, `/found`, `/connections`, and `/api/health` all returned `200`. The released JavaScript bundle is `index-BBHZaBNh.js` and contains the production Convex URL.
- In-app browser verification covered all four surfaces at 1280 px and 390 × 844. Every mobile route matched its viewport width; Me, Found, and Connections rendered live Convex state; Found placed Might at the right-hand door; Connections settled at opacity `0.64`; browser logs contained no warning or error.

### External effects, maturity, and remaining gate

- External changes were limited to the three approved production environment values, additive Convex schema/functions/components, and the static deployment. No application row, OpenAI request, Firecrawl request, AgentMail message, inbound event, approval, or connection state was created.
- The fixed-room UI and new backend function surface are now **公開部署已驗證**. AgentMail production inbound remains **未設定／未驗證** because production has no webhook secret or registered callback; this release is not a production Contact → Reply → Connected E2E claim.
- Backend rollback would deploy the previous reviewed commit; static rollback would republish the previous asset manifest. Environment rollback requires a separate explicit authorization and was not performed.

### Session close

- Ran the `/hackathon` workflow after deployment and public-browser verification. The public log records the static deployment ID, additive backend promotion, successful routes, and the deliberately absent production webhook boundary without exposing any credential or private data.

## Room-native Mighty settings

### Authorization and product boundary

- The owner requested settings for the Mighty name, API access, appearance, volume, background-music state, and adjacent personal preferences.
- Settings remains a drawer inside the fixed room rather than a fifth primary destination. Talk, Me, Might Found, and Connections remain the only primary surfaces.
- The existing BYOK safety decision remains binding: the anonymous public app does not collect an API secret. The drawer reports the active hackathon Sponsor mode and links to OpenAI's official key page while explaining that a future personal-key path requires sign-in and a server-only vault.
- Background music is not built, so its control is disabled and explicitly labeled unavailable rather than simulated.

### Implementation

- Added an accessible paper settings drawer to the shared app shell with desktop-rail and mobile-header entry points, Escape/focus handling, a Tab loop, reduced-motion behavior, and the approved room foreground as a low-opacity real asset.
- Added session-private companion settings in Convex. A backward-compatible optional name and appearance mode live on the existing anonymous session; indexed queries remain browser-session scoped. Name changes update Talk and companion labels reactively, while appearance can switch between the house orb and an already generated OpenAI form without deleting either.
- A new private session may open the existing manifestation prompt from the drawer. Completing a real generation makes that form active, while selecting an unavailable generated form fails closed.
- Replaced the decorative sound label with a working device-local sound preference, global switch, 0–100 volume control, and a subtle user-triggered two-note Web Audio preview. No audio preference or secret is sent to Convex.
- Added Lucide for interface icons rather than introducing text glyphs, custom SVGs, or CSS-drawn settings art.

### Verification and QA

- Convex codegen and `convex dev --once` synced the optional session fields and `companionSettings` functions to development `vibrant-wren-913` only.
- A synthetic browser session renamed the companion to `Aster`, observed `Hi. I’m Aster.` update live, then restored the same session to `Might`. The global sound label synchronized with the drawer switch; disabling sound disabled the slider and preview. The background-music switch remained disabled and the rendered drawer contained no password or API-key input.
- Desktop QA used 1280 × 900. Mobile QA used 390 × 844 and confirmed `scrollWidth === innerWidth`; both top and lower mobile drawer states retained the header and controls.
- The first scroll review found that browser auto-focus could programmatically scroll the otherwise hidden outer drawer and clip its header. The outer layer now uses non-scrollable clipping, the inner paper body owns scrolling, and explicit stacking keeps the header visible.
- The combined source-to-implementation review is `/Users/liuenyan/.codex/visualizations/2026/08/31/might-settings-drawer/source-vs-settings-final.png`; `design-qa.md` records P0, P1, and P2 as passed.
- Six test files / 26 tests, ESLint, TypeScript, Vite production build, `git diff --check`, development Convex sync, and browser console review passed.

### External effects, maturity, and rollback

- Development received only the additive optional schema fields and new settings query/mutations. Visual QA created/used one synthetic private development session and restored its companion name to `Might`.
- No production Convex function, schema, environment, row, static deployment, OpenAI request, Firecrawl request, AgentMail message, webhook, approval, or connection state changed.
- The drawer, device-local audio preferences, and development-backed identity/appearance controls are **本機已驗證**. They are not yet **公開部署已驗證**.
- Rollback removes the drawer/audio modules and the optional name/appearance reads. Existing production and development session rows remain readable because both schema fields are optional; existing Sponsor E2E state is unaffected.
