# Might worklog — 2026-08-28

## Session 0 — Public Convex foundation

- **Authorization:** user approved implementation, Convex setup/deployment, public GitHub creation/push, official-rule review, and use of installed Codex skills.
- **Inputs read:** the full 1,935-line `Might_Hackathon_Spec_v0.1.md`; selected AGENTS files from NOVAX, 初號機, and i-Depot 2.0; Matt Pocock `research`, `writing-for-agents`, `codebase-design`, and `tdd`; official Convex quickstart/expert guidance; the official All Gas and hackathon-log sources cited in `docs/research/all-gas-hackathon-rules.md`.
- **Governance:** `AGENTS.md` now freezes the one demo path, contest gates, evidence maturity, privacy/consent policy, visual direction, Static Hosting topology, and five public behavior seams.
- **Product:** built the responsive Talk, Me, Might Found, and Connections surfaces; the default animated orb; honest empty/integration states; mobile bottom navigation; reduced-motion support; and mute state. The interface does not render invented opportunities, replies, or sponsor results.
- **Convex:** created project `ranopha/might`; implemented indexed anonymous sessions, conversations, messages, status query, session mutation, message mutation, bounded realtime message query, API health route, and production Static Hosting with app routes reserved below `/api`.
- **Public runtime:** deployed `https://hushed-stork-401.convex.site` backed by `https://hushed-stork-401.convex.cloud`. Signed-out `/` and `/talk` returned HTML; `/api/health` returned live JSON; an unimplemented webhook POST returned `404`.
- **Browser E2E:** on local and production, Convex reached `live`; a synthetic user message was submitted, shown through the realtime query, and recovered after reload. Desktop and 390×844 Talk/Me/Found/Connections checks had no browser warnings or errors.
- **Verification:** `npm run lint`, `npm run typecheck`, `npm test` (1/1), and `npm run build` passed. Convex codegen, schema push, production deploy, and `talk:status` passed.
- **External changes:** created Convex dev deployment `vibrant-wren-913`, production deployment `hushed-stork-401`, and public GitHub repository `https://github.com/Ranopha/might`. No email, social post, final submission, or sponsor API call occurred.
- **Privacy:** the production verification used synthetic copy. `.env.local`, dependencies, and build output remain ignored; no provider secret is stored in source.
- **Architecture decision:** Static Hosting owns `/`; application HTTP routes appear under `/api`. A future AgentMail webhook will use an exact `/api/...` route and fail closed until signature and idempotency handling exist.
- **Rollback:** Git can revert the initial slice; Convex deployments and the GitHub repository are external resources and require separate explicit removal if ever requested.
- **Unknowns / gates:** OpenAI, Firecrawl, and AgentMail credentials and real calls; controlled inbound mailbox; Luma registration; sponsor credits; and final Vibe Apps form fields are unverified. The companion manifestation, semantic living memory, discovery, matching, consent, outbound, inbound, and Connected transition are not implemented yet.
- **Next tracer bullet:** OpenAI Talk reply plus source-linked memory candidate in Convex, with Me updating by realtime query.

## Session 1 — OpenAI Companion Manifestation and daylight visual pass

- **Authorization:** user approved reusing an existing OpenAI key, then explicitly chose to create/provide a new key when no usable value existed. The key was entered directly in a local terminal and stored in Convex environment configuration; its value was never read into chat, logs, source, or Git.
- **Credential correction:** presence checks proved that shell, workspace env, dev/prod deployment env, and dev/prod default env initially contained no OpenAI key. A CLI pipeline briefly wrote a non-secret error string into dev default env because `convex env get` returned a successful exit status for a missing value; it was immediately removed. The user later provided the real key through Convex's hidden terminal prompt. Dev default env was then copied without output into the existing `vibrant-wren-913` deployment because defaults do not backfill an existing deployment.
- **Manifestation backend:** added indexed `companionManifestations` records, lifecycle/error validators, client-request idempotency, anonymous-session isolation, trace fields, OpenAI model IDs/request IDs, and Convex Storage IDs. The public seam is `api.manifestation.generate` plus realtime `api.manifestation.current`.
- **OpenAI integration:** `gpt-5.6-luna` creates a strict structured art brief and adaptation note; `gpt-image-2` creates a 1024x1024 medium-quality PNG. Famous-IP references are reduced to mood and broad archetype before image generation. Only the Convex Storage ID is stored in the table; the client receives a resolved storage URL.
- **Runtime repair:** the first live attempt exposed a Convex default-runtime incompatibility in the OpenAI SDK (`Not implemented: set username for URL`). OpenAI I/O was moved into an isolated `"use node"` internal action. The public wrapper now catches unexpected cross-runtime rejection and settles it to `PROVIDER_ACTION_FAILED` instead of leaving a new request indefinitely generating. One pre-fix dev record remains as stale `generating_brief` evidence and was not rewritten.
- **Real integration evidence:** a fresh anonymous browser session submitted `A baby Batman who feels brave but gentle, like a tiny friend for late-night work.` The state advanced in realtime from `generating_brief` to `generating_image` to `ready`. OpenAI returned an adaptation note stating that the brave, gentle late-night feeling was retained while recognizable superhero elements were replaced. The original purple nocturnal guardian image was stored in Convex, rendered in the hero and compact navigation, survived reload, and followed the user into Talk.
- **Frontend:** added reusable generated-companion presence with animated orb fallback, truthful brief/image progress states, recoverable errors, private-session copy, and ready-state handoff into Talk. The four primary surfaces now use the generated companion when present.
- **Visual direction:** changed the product from a dark purple-black night theme to a bright ivory paper field with lilac and apricot ambient light, dark-plum typography, translucent story cards, softer shadows, and a daylight-lit orb. Talk, Me, Might Found, Connections, generated-art framing, navigation, focus, loading, empty, and mobile rules share the new tokens.
- **Verification:** `npm run lint`, `npm run typecheck`, `npm test` (2 files, 5 tests), `npm run build`, Convex-specific typecheck/dev push, `git diff --check`, ignored-env check, and redacted secret-pattern scan passed. Browser checks covered all four daylight surfaces, real generation, realtime stage changes, generated-art inspection, reload persistence, compact presence, and Talk handoff.
- **External effects:** updated only Convex dev `vibrant-wren-913`; executed one successful real OpenAI structured-response call and one successful image-generation call; stored one generated test image in dev Convex Storage. No production deploy, email, social post, or submission occurred.
- **Public boundary:** production `hushed-stork-401` and `https://hushed-stork-401.convex.site` still serve the previous public slice. A fresh production-deploy approval and production OpenAI environment setup are required before this manifestation slice can be called publicly verified.
- **Rollback:** Git can revert the code/theme slice. Convex dev manifestation rows, generated Storage blobs, and environment variables are external state and require separate targeted cleanup if requested; no such destructive cleanup was performed.
- **Next tracer bullet:** after production approval/deploy, implement Talk reply plus source-linked living-memory candidates and prove Me updates through a realtime query.
