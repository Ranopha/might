# Might worklog — 2026-08-28

## Session 0 — Public Convex foundation

- **Authorization:** user approved implementation, Convex setup/deployment, public GitHub creation/push, official-rule review, and use of installed Codex skills.
- **Inputs read:** the full 1,935-line `Might_Hackathon_Spec_v0.1.md`; selected AGENTS files from NOVAX, 初號機, and i-Depot 2.0; Matt Pocock `research`, `writing-for-agents`, `codebase-design`, and `tdd`; official Convex quickstart/expert guidance; the official All Gas and hackathon-log sources cited in `docs/research/all-gas-hackathon-rules.md`.
- **Governance:** `AGENTS.md` now freezes the one demo path, contest gates, evidence maturity, privacy/consent policy, visual direction, Static Hosting topology, and five public behavior seams.
- **Product:** built the responsive Talk, Me, Might Found, and Connections surfaces; the default animated orb; honest empty/integration states; mobile bottom navigation; reduced-motion support; and mute state. The interface does not render invented opportunities, replies, or sponsor results.
- **Convex:** created project `ranopha/might`; implemented indexed anonymous sessions, conversations, messages, status query, session mutation, message mutation, bounded realtime message query, API health route, and production Static Hosting with app routes reserved below `/api`.
- **Public runtime:** deployed `https://hushed-stork-401.convex.site` backed by `https://hushed-stork-401.convex.cloud`. Signed-out `/` and `/talk` returned HTML; `/api/health` returned live JSON; an unimplemented webhook POST returned `404`.
- **Browser E2E:** on local and production, Convex reached `live`; a synthetic user message was submitted, shown through the realtime query, and recovered after reload. Desktop and 390×844 Talk/Me/Found/Connections checks had no browser warnings or errors.
- **Verification:** `npm run lint`, `npm run typecheck`, `npm test` (1/1), and `npm run build` passed. Convex codegen, schema push, production deploy, and `talk:status` passed.
- **External changes:** created Convex dev deployment `vibrant-wren-913`, production deployment `hushed-stork-401`, and public GitHub repository `https://github.com/Ranopha/might`. No email, social post, final submission, or sponsor API call occurred.
- **Privacy:** the production verification used synthetic copy. `.env.local`, dependencies, and build output remain ignored; no provider secret is stored in source.
- **Architecture decision:** Static Hosting owns `/`; application HTTP routes appear under `/api`. A future AgentMail webhook will use an exact `/api/...` route and fail closed until signature and idempotency handling exist.
- **Rollback:** Git can revert the initial slice; Convex deployments and the GitHub repository are external resources and require separate explicit removal if ever requested.
- **Unknowns / gates:** OpenAI, Firecrawl, and AgentMail credentials and real calls; controlled inbound mailbox; Luma registration; sponsor credits; and final Vibe Apps form fields are unverified. The companion manifestation, semantic living memory, discovery, matching, consent, outbound, inbound, and Connected transition are not implemented yet.
- **Next tracer bullet:** OpenAI Talk reply plus source-linked memory candidate in Convex, with Me updating by realtime query.
