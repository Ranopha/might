# 2026-09-06 — approved browser recording

## First checkpoint — scope and authorization before email approval

The owner replied “核准” to the proposed local Playwright recording workflow.
This authorizes recording Might in a fresh isolated private room and preparing
video artifacts. The September 6 project freeze is respected: no application
source, dependency, backend configuration, deployed bundle or callback changed.
No social post, registration, login, account creation or submission was performed.
The two September 5 email approvals were already consumed; a new exact pair is
prepared in `docs/submission/recording-email-approval-2026-09-06.md` and awaits
specific approval. No new send or product Send approval was created.

## Actual recording and integration evidence

Used the bundled Playwright package and an already-installed Chromium executable.
The package's default expected browser revision was missing; the first launch
failed before creating a browser or room. Selecting the existing executable
worked. No browser, app dependency, global skill or plugin was installed/updated.

A fresh 1600 × 900 private context recorded the real production app at
`hushed-stork-401.convex.site`. The same new room completed original OpenAI Milo
generation, one natural Talk turn, four source-linked memories confirmed through
the UI, one live Firecrawl public volunteer page, OpenAI contextual matching,
one private clarification and an AI introduction draft. The generated Chinese
draft was edited to address the project-controlled inbox with explicit fictional
persona/test language. The source organization is not a participant or partner.

The exact revised payload hash and connection match the UI and the production
readback. There are zero send approvals, app mail rows, inbound events, reply
summaries and continuations for this room. Sanitized trace:
`docs/submission/evidence-2026-09-06-pre-consent.json`.

Recording closed safely at the consent boundary, flushing a 601.96 second VP8
WebM, 1600 × 900, 25 fps, 46,436,518 bytes. A fifteen-frame contact sheet verified
actual orb, form, generated companion, Talk, memory confirmation, source evidence,
clarification and complete draft/disclosures. Playwright recorded no app audio.
Its newly created room state is retained privately with mode 0600 for continuation;
it is not cloned from an existing user's browser or committed to Git.

Private recording files: `docs/submission/recording-2026-09-06.local/` (ignored).
`resume-recording.mjs`, with `MIGHT_RECORDING_PHASE=outreach`, reopens this same
recording room and writes a separate video and event log. It must only continue
past consent after the owner approves the new reviewed messages. Provider reply
helper also fails closed without a matching local authorization record, and
preserves a stable idempotency key/intent before any authorized reply.

## Video production

An independent video task prepared the editable 160-second HyperFrames timeline
under `videos/might-demo.local/` (ignored), using existing HyperFrames 0.8.25.
No product UI was reconstructed. The source ledger binds video ranges to a
SHA-256 digest; final output rejects missing footage and out-of-range selections.

Nine English narration files were generated with the existing project OpenAI
credential, `gpt-4o-mini-tts` and built-in `marin` voice. Actual receipts/durations
are retained locally. Total narration is 86.05 seconds; every segment fits its
allocated scene. Opening disclosure includes fictional persona, controlled
inboxes and AI narration. No voice was cloned, subscription added or music used.
Template checks passed runtime/layout/contrast, with 40/40 text checks; these are
composition-template results, not final footage acceptance. HyperFrames created
its font cache on its first check; subsequent cache paths stay inside the video
folder. Subtitle sentence timing and the actual assembled export need separate
verification; no full listening review is inferred from metadata.

## Open gates and recovery

At this checkpoint the new exact two-message approval remains pending. Keep the
existing draft/hash and private recording room intact. Do not start a fresh room
or regenerate sponsor outputs merely to resume the remaining shots. The same
Connections page must be captured through genuine outbound receipt, signed
inbound reply, live state update and the approved Connected continuation.

Final <3-minute video, signed-out playback, registration/account verification,
approved social publishing and final contest submission remain open. No second
complete production E2E or finished hackathon entry is claimed.

The pre-existing untracked research note was preserved. The literal `/hackathon`
interface is unavailable; the installed skill and log-format workflow are applied
manually when updating the public build log, without claiming slash execution.

Narration verification: transcribed all nine clips with word timestamps and
aligned the result to the script. The first place-name pronunciation was
ambiguous in transcription, so only that sentence group was regenerated with
“Taiwan”; its new transcription matches the script. Selected narration now totals
84.60 seconds. Subtitle cues use ASR-aligned word times, not an unperformed human
listening review. Existing audio files and receipts remain available for provenance.

The public app was read back after recording. Its `index-B0joWhAX.js` bundle
is byte-identical to the existing local production build; the source checkout
remains `09dda1f`. The bundle hash is in the sanitized recording trace.

## Delivered pre-consent preview

Rendered `videos/might-demo.local/pre-consent-review/renders/Might-PRE-CONSENT-REVIEW.mp4`: 124.000 seconds, 1920 × 1080, 30 fps, H.264/AAC 48 kHz,
52,311,501 bytes. Render and complete ffmpeg error-on-decode validation exited 0.
Reviewed 29 cut-boundary snapshots and a sixteen-frame rendered contact sheet,
plus full-size draft/end views. No missing/black frames or caption obstruction
were found in these checks. Final digest and verification receipt are recorded
in `docs/submission/video-preview-2026-09-06.json`.

The preview includes 15 real source ranges at normal speed, shortens elapsed
waits transparently, identifies the manually edited controlled-inbox draft and
shows “PRE-CONSENT REVIEW · NO EMAIL SENT”. It ends before approval; the final
36 seconds and a final submission movie remain pending. The original 160-second
editable project, narration, source footage and evidence remain preserved.
The video is local only; no upload/public video link or final submission occurred.

Release checks: the six staged documentation/evidence files contained no matches
to three known local sponsor secret values or the room bearer-key field.
Application/Convex source and app package manifests have no diff. The pre-existing
research note remains untracked. Media/session directories remain ignored.

## Approved outreach continuation — second production E2E

The owner replied “核准” to the exact September 6 two-message document. Recorded
that authorization privately at 10:24 UTC, restored only this recording room's
own saved state and checked the recipient, complete authored body, disclosed
fields and payload fingerprint against the approved document. September 5's
consumed approval was not reused. No extra recipient or nonprofit was contacted.

At the first preflight, a decorative consent note overlapped the approval button's
center at 1600 × 900. The normal click timed out before any approval/send. That
92-second attempt remains preserved separately. The final recording used a normal
click on the visible upper-left part of the same button; no force click, DOM
mutation or application edit was used. This layout defect remains open under the
current application freeze.

One product approval and one separate Send action produced an AgentMail receipt.
Provider readback confirmed exactly one introduction sent and received with the
approved authored content plus the disclosed service footer. The controlled partner
helper persisted an intent and stable idempotency key before sending the one
approved reply. Readback confirmed one reply for that subject and parent message,
the expected recipients, authored content, service footer and quoted original.

The same Connections page changed from “Your hello is on its way.” to “They
replied.” without navigation or refresh. OpenAI's reply-excerpt summary appeared,
then the approved “Yes, help me continue” action reached “A possibility became a
real hello.” The app persisted one send approval, mail thread, inbound event,
completed reply summary and continuation, with matching message/thread/event IDs.
The official AgentMail component reports this outbound as delivered. Both app
mail and connection are connected. New public evidence:
`docs/submission/evidence-2026-09-06-connected.json`. The pre-consent checkpoint
and September 5 proof remain unchanged.

The final outreach source is 147.800 seconds, 1600 × 900, 25 fps, VP8 with no app
audio, 13,013,901 bytes; SHA-256
`6bf64cb5c2559075ab92e85d36c884cdfa155e81fbe7ba91f7fe2113d38b70b2`.
It includes the approval, send, real incoming transition, summary and Connected.
Browser events contain no page errors or failed commands in that final take.
The source is retained privately with the original 601.96-second pre-consent take.

Verification commands were the recording controller, recording-mail (readback,
then the authorized single reply), collect-evidence, verify-reply and
sanitize-connected, all in the ignored recording directory. The last collector
completed at 10:28:39 UTC. Assertions passed exact approval/body/hash, linked
source/memory/match and mail/event/summary/continuation IDs, chronological action
ordering, one email per approved direction and delivered component status.
The isolated repetition verifies a second real production E2E, still using a
fictional persona and controlled inboxes. It is not commercial or nonprofit
acceptance. No app source, dependency, provider configuration or deployment changed.

At 10:34:04 UTC the public app returned HTTP 200 without credentials and its
JavaScript still matched the recorded production SHA-256. GitHub reports
`Ranopha/might` as PUBLIC. These readbacks are separate from later signed-out
video playback and final form submission, which have not occurred.

## Complete local video delivery

The independent video task completed the last 36 seconds using six source
ranges at 1×, for 21 real ranges total. The continuous 53.6–59.6 second outreach
range retains the actual reply transition: source frame 56.32 still says “Your
hello is on its way.” and 56.36 says “They replied.” The final timeline places
that transition at approximately 142.76 seconds. Shortened waits are labeled.
The final composition discloses the fictional persona, controlled inboxes and AI
narration, and removes preview/draft watermarks. The earlier 124-second preview,
its accepted edit/caption contract and all nine voice files remain unchanged.

Rendered `videos/might-demo.local/renders/might-demo.mp4`: 160.000 seconds,
1920 × 1080, 30 fps, H.264 yuv420p / AAC 48 kHz, 67,072,164 bytes. Render exited
0 and stopped after 306.9 seconds. Full video/audio error-on-decode validation
exited 0 with 4,800 decoded frames. Audio peak is -4.5 dBFS. Caption timing was
checked against the ASR-verified text; no human listening acceptance is claimed.
Checked 18 ending/cut composition snapshots, an actual-export sixteen-frame
contact sheet and full-size reply/Connected frames. The primary task independently
read the actual transition and closing frames and verified the file hash.
Sanitized delivery receipt: `docs/submission/video-final-2026-09-06.json`.

No further model audio was generated, mail sent, application change made or
deployment performed while assembling the final movie. All editable source and
private recording/evidence remain in their ignored directories. Recovery means
re-editing or re-rendering these retained inputs, not recreating sponsor events.
Both two-message authorizations are consumed. Local video completion does not
complete public playback, registration/eligibility, social publication or final
submission. Social platform preference was requested; none has been selected or
published in this continuation.

A possible header crop seen in the tool's whole-image presentation triggered a
narrow follow-up. Fresh MP4 and saved-PNG header RGB bytes match exactly (zero
channel differences). All 43 consecutive frames from 142.6 to 144.0 seconds keep
the same full glyph bounds and at least 19 pixels of top margin. Enlarged header
strips are complete. The actual artifact is intact; the precise preview display
cause is unknown, so no composition change or re-render was needed. The digest
is unchanged and the public receipt includes this follow-up result.

Release scope is eight documentation/evidence files. Whitespace checks pass;
application/Convex source, package manifests and lockfiles have no diff. Known
local credential values and the private session key are checked against staged
content without printing them. Media and session directories remain ignored;
the pre-existing untracked research note is left untouched. No application test
rerun is claimed for this media/documentation-only continuation.


## Authorized publication and final-entry continuation

The owner explicitly requested completion through final submission, including
social publication on the X and Threads pages in the existing external Chrome
CODEX group. This authorizes the scoped publishing continuation and the necessary
consent-control layout polish. It does not renew either consumed test-email
approval or authorize paid upgrades.

A bounded independent task reproduced the decorative note's desktop overlap in
an offline fixture. The -92px margin and stacking order placed it 58px over the
letter. Changed only `.surface-story-screen > .editorial-note--paper` to
`margin-top: 0`, retaining the mobile override and empty-state footer. Fourteen
cases (seven widths, approval/send states) passed nine-point hit testing,
ordinary fixture clicks exactly once and zero horizontal overflow. Lint,
typecheck, production-URL build and diff checks exited 0. No provider or email
operation occurred. The fixtures cannot demonstrate sponsor runtime.

Applied the deployment-guard target classification. `.env.local` names personal
dev; the read-only Static Hosting production getUrls query confirmed
`hushed-stork-401`. The explicit completion/publishing request supplies the
session's deployment scope. Published the already verified production bundle
with `npx @convex-dev/static-hosting deploy --skip-convex --skip-build`.
Release `ec8f8868-08a3-4d91-90e2-a1e41356cfab` completed in 14.4 seconds. The
hosting CLI uploaded sixteen assets and performed its built-in old-asset cleanup.
No Convex backend deployment or route/configuration change occurred.

Public HTTP readback at 11:08:17 UTC returned 200; JavaScript and CSS exactly
match local build bytes. JavaScript SHA-256 is unchanged from the recorded
release, despite its new manifest filename. Receipt:
`docs/submission/consent-layout-release-2026-09-06.json`. Chrome restored its own
prior room, while a newly opened Incognito window displayed only the initial orb
welcome and Convex live. Recovery is a single-hunk revert, production build and
frontend republish; no data migration or rollback is needed. The original video
and immutable traces remain unchanged.

Published one X post and one Threads post using CUA on the actual Chrome
accounts. Read back both resulting post URLs and the full content. The X post
has the four real sponsor mention links and #AllGasHackathon. Threads carries a
Traditional Chinese explanation with the controlled-demo boundary. Each uses the
verified video's opening frame with descriptive alternative text. Exact text
and links are in `docs/submission/publication-2026-09-06.md`. No promotional
spending, synthetic engagement or unsolicited direct message was used.

Signed into Vibe Apps and Luma using the owner's personal Google account with
basic profile/email scope only. Skipped optional phone and passkey setup. The
Vibe Apps form has product text, public app/repo/X links, the actual screenshot,
convex.site selection and six relevant tags prepared. A screenshot confirmed the
GitHub URL and selected filename: AX/DOM-clone values omit some uncontrolled
inputs, so their empty readback was not treated as application failure.

The YouTube upload modal explicitly says uploading agrees to its terms and
community guidelines. Requested the browser-required action-time confirmation
for the exact verified MP4 on the existing channel, plus entrant eligibility and
identity confirmation. Those answers remain pending at this checkpoint. No
YouTube upload, Luma registration or Vibe Apps submission is yet claimed.

The installed hackathon skill's workflow is applied manually; the slash-command
interface is unavailable and no `/hackathon` invocation is claimed.


### Hosted video and registered entrant

The owner explicitly accepted the specific YouTube upload terms and confirmed
solo-entry identity and eligibility. Uploaded the unchanged 67,072,164-byte MP4
(SHA-256 c6fb798ad65ae5a3618463c451131fbaa88408afdbcfac5d0a3a1878ce559a47).
YouTube displayed Video published with URL https://youtu.be/VjZp9nPWEEU, unlisted.
Saved title/description, AI-content disclosure, not-made-for-kids audience,
English language, verified real opening-frame thumbnail and imported timed SRT.
The public Incognito player shows Sign in and plays the actual product without
an account, reporting 2:40. Studio's rounded 2:41 preview also satisfies the limit.
YouTube's copyright/community checks completed with no issues found. No paid
promotion or account/subscription upgrade occurred.

Luma registration completed with the owner's personal account and X handle; the
page explicitly displayed 報名成功. Ticket tokens and private contact details are
not copied to this log. Vibe Apps has the approved name/team/contact details and
hosted video URL, with final submission not yet performed at this checkpoint.

The independent factual review found the prepared "only confirmed memories"
claim too strong: candidates persist privately as active before confirmation,
and matching may use them. Corrected the video description before publication.
Threads' original shorter wording was clarified with a public follow-up at
https://www.threads.com/@ranopha/post/Dc8bWmGjz5r after its edit window expired.
The original post and exact history are retained in the publication record.

CUA's browser bridge timed out and reset, then reported Debugger unattached for
existing tabs. Continued through the supported native Chrome UI in CUA. No shell,
CDP, account-cookie export or alternate technology controlled the user's browser.
