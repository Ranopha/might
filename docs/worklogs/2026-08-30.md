# Might worklog — 2026-08-30

## Production World Sensor / Match promotion

### Authorization and target

- The owner prepared the production Firecrawl credential in their own macOS Terminal and replied `好了` to continue the previously approved production deployment.
- Target: Convex production `hushed-stork-401` (`ranopha:might:production`).
- Scope: deploy the existing backend from current `main`; no static frontend release, Sponsor runtime call, email send, application-data mutation, or further environment change was included.

### Pre-deploy evidence

- `HEAD` and `origin/main` were both `e2db973f74f16f2ba2a41ca83ae25b7a634f1116` with a clean working tree.
- The files under `convex/`, `src/`, `package.json`, and `package-lock.json` had no diff from the previously reviewed backend checkpoint `4fc1a1d`; later commits were documentation-only.
- Production `FIRECRAWL_API_KEY` presence was checked without printing its value. No secret was copied into Git, logs, screenshots, or this worklog.
- `npm test -- --run`: 4 files and 17 tests passed.
- `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`: passed.
- Production dry-run completed schema validation and reported `No indexes are deleted by this push`. It proposed only the Node.js actions server-version update.

### External effect and verification

- `npx convex deploy --yes --typecheck enable` completed against `hushed-stork-401`, deleted no indexes, installed the Firecrawl and rate-limiter components, and activated the Convex functions.
- Production function inventory now includes browser-session guards, Manifestation, World Signal scan, Match request/dismiss, and clarification submit/re-judge functions.
- `https://hushed-stork-401.convex.site/api/health` returned `{"status":"live","service":"might-api"}` after deployment.
- A post-deploy dry-run again completed schema validation and reported zero index deletion. The CLI still described a Node.js actions server-version overwrite even after the successful deploy; runtime functions are present, but that repeated informational diff remains an observation to watch on the next Convex CLI update.
- No OpenAI, Firecrawl, AgentMail, or outbound-email call was made during this deployment/verification session.

### Maturity, rollback, and next gate

- The anonymous paid-work guards and the World Signal → Match → Clarification backend are now **公開部署已驗證** by production inventory and health checks.
- This does not yet prove a fresh production Firecrawl/OpenAI browser flow. The prior real Notice/Match/Clarification integration proof remains development-only until one controlled production tracer completes with provider receipts and reload persistence.
- AgentMail, payload-bound consent, inbound webhook verification, `REPLIED`, `CONNECTED`, and the full real E2E remain unimplemented or unverified and are not claimed.
- Rollback: redeploy the last known production code revision if the new functions regress. Removing or rotating the production Firecrawl credential is a separate external-state action and requires explicit authorization.

## Public production Sponsor tracer

### Authorization and release

- The owner explicitly approved: `同意部署 production 前端並執行一次 Sponsor tracer`.
- The current frontend source had no diff from reviewed checkpoint `4fc1a1d`; 17 tests, lint, typecheck, and the production build passed before release.
- `@convex-dev/static-hosting deploy --skip-convex` published production static deployment `9c72865c-c2a9-43a9-9c0a-5bd9906516c9` to `https://hushed-stork-401.convex.site`. The command rebuilt with `https://hushed-stork-401.convex.cloud`, skipped backend deployment, atomically published three files, and cleaned three superseded static files.
- Rollback for the static release is a rebuild and publish from the prior Git checkpoint; no backend schema, function, or environment rollback is required for this frontend-only release.

### Real Talk → Remember → Notice → Match evidence

- A browser-scoped synthetic persona stated ten years of woodworking, ownership of tools, willingness to volunteer on small Taoyuan community repairs, and Saturday availability. This was demo data, not owner personal data.
- The public Talk UI persisted the message immediately; OpenAI replied naturally, and four source-linked private memory candidates appeared in Me through realtime queries. All four were explicitly confirmed in the test session.
- From the public Might Found UI, one allowlisted source scan produced a live Firecrawl result from the Taoyuan nonprofit volunteer page. OpenAI separated evidence from inference, Convex saved the World Signal, and the UI displayed the source excerpt and URL.
- A separate OpenAI Match Judge used only the four confirmed session memories and produced a strong possible fit. Because willingness and availability were already explicit, no clarification was needed.
- The persisted Match displayed `No consent requested`, `Reasoned, not shared`, and `Might cannot contact anyone`; no Interest, approval, AgentMail, external contact, or email action occurred.

### Runtime and browser verification

- Production logs linked `talkOpenai:generateTurn` (about 9.39 seconds), Firecrawl component `lib:scrape` (about 4.04 seconds), `worldSensor:scanSource` (about 8.93 seconds), and `matchJudge:judge` (about 3.67 seconds) to their deterministic request/commit functions.
- A full reload restored the same source, Match reasoning, memory evidence, and consent boundary without a second provider action.
- Desktop and 390px browser inspection showed the daylight editorial layout and all Sponsor receipts; the browser console contained no warning or error.
- Maturity is now **公開部署已驗證** for Talk → Remember → Notice → Match. The complete E2E remains open at Interest, contextual pitch, payload-bound Send approval, AgentMail outbound/inbound, Replied, and Connected.

## Interest → contextual pitch → payload-bound consent slice

### Scope and design boundary

- Continued only the next primary-path slice after the authorized public tracer. The new code was synced to development `vibrant-wren-913`; production `hushed-stork-401` was not changed again.
- `I’m interested` is explicitly not Send consent. It creates one browser-session-owned private pitch intent and schedules a real OpenAI Responses action only for a new surfaced Match.
- OpenAI receives untrusted public signal/match context plus at most four relevant memory candidates under instructions that forbid invented facts, recipient details, commitments, or command-following from source data. New pitch requests set `store: false`; the initial recipient is always `null / unavailable`.
- Recipient, subject, body, selected memory snapshots, private fields, revision, provider receipt, SHA-256 payload hash, approval timestamp, connection, recipient, and idempotency key are persisted separately. A recipient/body change or forgotten source memory invalidates the old approval.
- Approval still returns `canContact: false` and `sendCount: 0`. No AgentMail dependency, inbox, webhook, outbound record, email send, or inbound state transition was added in this slice.

### Product behavior and visual work

- Might Found now presents `I’m interested` beside `Not for me`, with the explicit copy `Creates a private draft · does not send`.
- Connections now reads Convex state in realtime and presents the journey as a bright editorial letter rather than a dashboard: drafting state, actual recipient status, full subject/body, exact private fields, payload fingerprint, revision editor, expired-approval warning, and an exact-message approval control.
- Recipient and copy are revised before approval. The UI reads the resulting new hash and only then permits a separate approval mutation; the same click never changes payload and approves it.
- Empty Connections was visually inspected on desktop and a 390 × 844 responsive viewport. Both retained the daylight storybook palette, readable consent path, and mobile navigation.

### Deterministic and runtime verification

- Five new behavior tests cover interest without consent/contact, complete disclosure preview, revision/hash/approval idempotency, recipient/body invalidation, cross-session isolation, and Forget fail-closed behavior.
- Full verification passed: 5 test files / 22 tests, ESLint, TypeScript, Vite production build, `git diff --check`, and a repository secret-pattern scan.
- `npx convex dev --once` completed twice against development `vibrant-wren-913`, including the final `store: false` OpenAI adapter. The new empty-state frontend was opened from the local Vite build and inspected through the in-app browser.
- No live OpenAI contextual pitch was requested in this session, because the single authorized production Sponsor tracer had already completed through Match. The populated draft/consent UI therefore remains implemented and deterministically tested, not yet real-integration verified.

### AgentMail research and next gate

- Official AgentMail documentation and the official `@agentmail/convex` component were reviewed. The planned callback is `https://hushed-stork-401.convex.site/api/agentmail/webhook`, preserving Static Hosting at `/` and application HTTP routes under `/api` (`docs/research/agentmail-integration-2026-08-30.md`).
- The next gate requires a real authorized recipient, AgentMail credentials/inbox/webhook configuration, one live OpenAI pitch, explicit in-product approval of the exact revised payload, and then a separate AgentMail outbound/inbound tracer. External AgentMail assets and email side effects were not created or authorized in this session.
- Rollback for this development-only slice is to redeploy the prior Git checkpoint to dev. Production requires a fresh exact deploy approval after the live pitch/consent evidence is reviewed.

### Session close

- The `/hackathon` workflow was run after verification. `hackathon.md` records the production Sponsor tracer and the development-only consent slice without claiming a live pitch, production consent deployment, AgentMail, reply, Connected, or full E2E completion.

## AgentMail outbound / inbound / realtime connection seam

### Scope and authorization boundary

- Continued the next primary-path slice in development `vibrant-wren-913`. Production `hushed-stork-401` was not changed.
- Installed and mounted the official `@agentmail/convex` component. The development AgentMail API credential and inbox identifier were set in Convex without printing their values; neither secret nor the real identifier was added to Git or this log.
- A read-only AgentMail account check found zero inbox webhooks. No webhook was created, no email was sent, no recipient was contacted, and no production environment value was changed.
- Added a required server-side controlled-recipient allowlist. It remains unset in development, so the live send path currently fails closed even if a browser produces a valid approval.

### Product and backend behavior

- Added the separate `Send this exact email` action after approval. Convex revalidates the owning anonymous session, latest approval, recipient, subject, body, payload hash, selected evidence, inbox configuration, and demo-recipient allowlist before the official component may enqueue a send.
- One approval owns one durable mail binding and one component outbound ID. Retrying the same or a new client request against that approval returns the existing record; it cannot enqueue a second message.
- Outbound provider status is synchronized into `queued`, `sent` / `delivered`, or a fail-closed delivery state. A real AgentMail message ID and thread ID are required before the connection becomes `contacted`.
- Mounted the official Svix-verifying component handler at the application route `/api/agentmail/webhook`. Its internal callback accepts only the configured inbox, a recipient list containing that inbox, and an existing contacted AgentMail thread; unknown threads and duplicate event IDs do not change the connection.
- A verified bound inbound event stores a bounded reply preview and moves `contacted → replied`. The owning browser receives the state through the existing realtime query. `connected` requires a later explicit `Yes, help me continue` action and is defined only as two-way contact, not an agreement or transaction.
- Connections gained bright editorial delivery receipts, reply motion, a six-step progress path, explicit failure copy, and responsive connected-state treatment. Desktop and 390 × 844 empty-state geometry were inspected without browser warnings or errors.

### Verification, maturity, and rollback

- Red-to-green tests first proved the missing recipient restriction, then proved that unallowlisted addresses create zero sends. The expanded suite also covers stale approval, exactly-once outbound enqueue, real provider receipt binding, unknown and duplicate inbound events, realtime `Replied`, and idempotent explicit `Connected`.
- Full verification passed: 5 test files / 24 tests, ESLint, TypeScript, Vite production build, Convex codegen, development function sync, `git diff --check`, and a repository secret-pattern scan.
- Maturity is **已實作 / 本機已驗證** for the complete outbound/inbound seam and **development component installed** for AgentMail. It is not yet a real AgentMail integration proof because no webhook, live outbound, live inbound, or populated-browser tracer ran. Production and the full E2E remain open.
- Rollback is to redeploy the prior Git checkpoint to development. Removing the development component, rotating credentials, creating a webhook, enabling an allowlisted mailbox, deploying production, or sending a message are separate external-state actions.

### Next exact gate

- Obtain explicit permission to create one inbox-scoped `message.received` webhook pointing to `https://vibrant-wren-913.convex.site/api/agentmail/webhook`, storing its returned signing secret only in Convex development.
- Name one owner-controlled recipient mailbox for the short allowlist. Then run one real development tracer through live OpenAI pitch, exact in-product approval, AgentMail outbound, a human reply, Convex realtime `Replied`, and explicit `Connected` before proposing any production promotion.

## Live contextual pitch and AgentMail development callback

### Authorization and external effects

- The owner explicitly approved all three preparatory actions: use one owner-controlled mailbox as the development allowlist, create one inbox-scoped `message.received` webhook for the development callback, and send the existing synthetic Match context to OpenAI with `store: false` to produce the contextual pitch.
- AgentMail created exactly one enabled inbox-scoped webhook targeting `https://vibrant-wren-913.convex.site/api/agentmail/webhook`. A read-back confirmed the exact URL, event type, enabled state, and idempotent client identifier.
- The returned webhook signing secret was written only to Convex development `vibrant-wren-913`; the single controlled-recipient allowlist was also set only in development. Neither value was printed, committed, or promoted to production.

### Real OpenAI pitch and current consent boundary

- Reused the existing synthetic development session containing a live Firecrawl public signal and OpenAI Match. Selecting `I’m interested` created one private pitch intent and ran the real OpenAI contextual-pitch action.
- Connections updated from the drafting state to a complete Chinese-language pitch without refresh. It displayed the full recipient, subject, body, one selected synthetic private-memory statement, and a new SHA-256 fingerprint after the controlled recipient was saved.
- The browser is stopped at `Approve this exact email`. The current payload has not been approved, the separate `Send this exact email` action has not appeared, and the AgentMail send count remains zero.
- No test email, outbound thread, inbound reply, `Replied`, or `Connected` transition is claimed yet. Production `hushed-stork-401` was not changed.

### Next exact gate

- Obtain action-time approval of the displayed recipient, subject, complete body, selected private field, and fingerprint. Only then may the browser record approval and expose the separate Send action; the actual Send remains a second representational action and must use that exact approved payload.
- After the owner replies from the controlled mailbox, verify the signed webhook receipt, AgentMail message/thread IDs, one Convex inbound event, realtime `Replied`, reload persistence, and explicit `Connected` before recording real AgentMail E2E.

## Exact consent to one real AgentMail outbound

### Consent and exactly-once evidence

- The owner reviewed the displayed recipient, complete Chinese subject/body, selected synthetic private-memory statement, and SHA-256 fingerprint, then explicitly authorized both approval and sending of that exact payload.
- Immediately before approval, the browser re-read every disclosed field and the fingerprint; all matched the reviewed payload. The browser recorded approval first, then used the separately rendered `Send this exact email` action once.
- Convex created one application mail binding and one official component outbound. The UI showed `1 approved outreach`; the Send control disappeared and was never clicked again.

### Runtime failure, diagnosis, and repair

- The first official workpool attempt failed closed before any AgentMail API message existed. Runtime logs showed that `@agentmail/convex@0.1.0` could not see `AGENTMAIL_API_KEY` inside its isolated component runtime even though the key was set on the parent app deployment.
- Official Convex component documentation confirms that components cannot read parent app environment variables unless the component declares them and the app passes explicit references. The published AgentMail component read `process.env.AGENTMAIL_API_KEY` but did not declare this environment input.
- Added a reproducible `patch-package` patch for `@agentmail/convex@0.1.0` that declares `AGENTMAIL_API_KEY` and optional `AGENTMAIL_BASE_URL`. Might's `convex.config.ts` now maps those values from typed app environment references. No secret appears in the patch, Git, logs, or generated code.
- Synced only development `vibrant-wren-913`. The same pending work item remained at one outbound and retried after the repair; no second send intent or approval was created.

### Real provider and realtime evidence

- The existing outbound's final durable retry succeeded. AgentMail API reported exactly one sent message with the approved subject and controlled recipient.
- The official component and Might's `mailThreads` record hold the same real provider message ID and thread ID, `status: sent`, `sendCount: 1`, the approved payload hash, and no error.
- Connections updated without refresh from `AgentMail is delivering` to `Reached out through Might` and displayed the real-thread receipt state.
- A clean `npm ci` reapplied the AgentMail patch automatically. Afterward, 5 test files / 24 tests, ESLint, TypeScript, Vite production build, and dependency audit all passed with zero known vulnerabilities.
- No production code or environment changed. The owner still needs to reply from the controlled mailbox; inbound webhook processing, realtime `Replied`, reload persistence, explicit `Connected`, and complete E2E remain open.

## First real inbound reply and Replied state

### Signed inbound evidence and diagnosis

- The owner replied from the controlled mailbox in the existing AgentMail thread. No new outbound, approval, recipient, webhook, or production change was created.
- Development logs show one successful `POST /agentmail/webhook` with HTTP `204`; the official component persisted one `message.received` event and one inbound message. Its inbox and thread identifiers matched Might's sent binding, and the sender was external to the AgentMail inbox.
- Might remained `contacted` with zero application inbound rows because the real event represented addresses as `Display Name <email>`, while the application guard compared each complete display string to a bare inbox address. The canonical mailbox values matched; the fail-closed rejection was therefore caused by formatting, not by a foreign inbox or thread.

### Red-to-green repair and development sync

- Changed the existing inbound behavior test to use the provider's display-name address shape. It failed red with `invalid_message`, then passed after `agentMailInbound.ts` began extracting canonical mailbox addresses only for identity comparisons.
- The guard still requires bounded strings, a valid mailbox address, exact inbox/thread binding, a recipient containing the configured inbox, an external sender, contacted state, a real outbound receipt, and a new event ID. Raw bounded address strings remain stored for UI/audit.
- Full verification passed: 5 files / 24 tests, ESLint, TypeScript, and the Vite production build. Convex codegen and `convex dev --once` synced only development `vibrant-wren-913`.

### Reconciliation, realtime proof, and remaining consent

- AgentMail had already received `204`, so it did not redeliver the event after the fix. The one already Svix-verified event persisted by the official component was passed once to the corrected internal mutation. This created one application inbound event and changed both the mail binding and connection from `contacted` to `replied`; `sendCount` remained exactly one.
- The Connections screen that was already open changed without refresh to `They replied`, displayed the real reply, and exposed the separate `Yes, help me continue` decision. A full reload preserved `Replied` and the same one-outbound state.
- The continuation button was not pressed. `Connected` still requires fresh explicit owner consent. Production `hushed-stork-401` remains unchanged, and a future unassisted post-fix webhook event is still required before calling the current callback path fully reverified without reconciliation.

## Explicit Connected transition

### Authorization and external boundary

- The owner explicitly wrote `同意 Connected` for the one current `Replied` connection. This authorized only the existing in-product continuation decision; it did not authorize another email, a commitment, price, payment, agreement, or schedule.
- Target: development `vibrant-wren-913`. Production `hushed-stork-401`, Convex environment values, the AgentMail webhook, and external mail state were not changed.
- The browser re-read the current `They replied` state and used the single `Yes, help me continue` control once. No send control was present or invoked.

### Realtime, persistence, and exactly-once evidence

- The already-open Connections screen changed without refresh to `A possibility became a real hello.` and `Connected`. The displayed definition says that a two-way conversation began and explicitly does not imply a promise or agreement.
- A full page reload restored the same `Connected` state. The continuation control did not reappear.
- Redacted Convex development reads found exactly one `connectionContinuations` record, one `mailInboundEvents` record, one `mailThreads` record with `status: connected`, `sendCount: 1`, provider message/thread receipts present, and no error, plus one `connections` record with `status: connected`.
- No second outbound email was sent. This real development path has now reached Connected, with the previously disclosed one-time reconciliation between the verified AgentMail component event and Might's corrected inbound mutation.

### Maturity and remaining gates

- The single development evidence chain now reaches `Talk → Remember → Notice → Match → Consent → Contact → Reply → Connected` with real OpenAI, Firecrawl, AgentMail, Convex persistence, and realtime UI behavior.
- This is not yet full hackathon acceptance: the corrected inbound callback still needs one unassisted post-fix webhook run, the entire path needs a clean repeatability rehearsal, and the currently development-only AgentMail/Connected slice needs a separately approved production promotion and public-browser proof.

## Three-run repeatability rehearsal — pre-send gate

### Scope and isolation

- The owner asked to run the complete path three times. Three new browser-scoped demo sessions were created on distinct localhost origins so that memories, manifestations, signals, matches, drafts, and consent state remained isolated without reading or manipulating browser storage.
- Target: development `vibrant-wren-913`. Production `hushed-stork-401` was not changed.
- Each session generated a different original Webtoon-style companion through OpenAI and Convex Storage, saved one natural Talk turn, produced and confirmed four source-linked private memories, interpreted the public Taoyuan volunteer source, ran an OpenAI contextual Match, answered one private clarification, and produced a contextual pitch.

### Sponsor and consent evidence

- Runs 1 and 3 displayed `Firecrawl live`; run 2 displayed `Firecrawl cached` and was kept honestly labeled. All three performed new OpenAI conversation, memory, world interpretation, Match, clarification re-judge, and pitch work.
- Each draft was revised to the same controlled allowlisted mailbox, given a run-specific subject, and saved with a new payload fingerprint. All three stopped at `Approve this exact email` with `Nothing sent`.
- Redacted Convex reads found three new `pitch_ready` connections. The existing totals remained one send approval, one mail thread, and total `sendCount: 1`, all belonging to the earlier completed tracer; the three rehearsal drafts added zero approvals, zero mail threads, and zero emails.
- The next gate is explicit owner approval of each displayed recipient, complete subject/body, private-memory disclosure, and fingerprint. Only then may each session record approval and expose its separate Send action.

## Three-run exact-payload outbound completion

### Authorization and execution

- The owner explicitly authorized approval and sending of the three previously disclosed exact payloads. This did not authorize a fourth email, any edited payload, a reply, or a `Connected` transition.
- Target: development `vibrant-wren-913`. Production `hushed-stork-401`, its environment, and the public frontend were unchanged.
- Immediately before each approval, the browser re-read the recipient, complete subject/body, disclosed private-memory fields, and SHA-256 fingerprint. All three still matched the authorized payloads exactly.
- Each isolated session used `Approve this exact email` once and then the separately rendered `Send this exact email` once. No send control was retried.

### AgentMail and Convex evidence

- All three Connections screens reached `Reached out through Might` and reported a real AgentMail thread receipt. One screen briefly showed the locked delivering state and then completed without another click.
- Redacted Convex reads found four total approval records and four total mail bindings across the deployment: the earlier completed tracer plus these three new runs. Total `sendCount` is exactly four.
- All four mail bindings have provider message/thread receipts, zero recorded errors, and statuses of one `connected` plus three `sent`. Connections contain one `connected` plus three `contacted` records.
- This proves three additional exactly-once Consent → Contact runs. It does not yet prove their inbound path: the owner must reply separately in all three provider threads before unassisted signed webhook delivery, realtime `Replied`, and any later explicit `Connected` decisions can be assessed.

### Public-log and rollback boundary

- The public hackathon log records only redacted counts and maturity. It omits the controlled mailbox, message content, private memories, payload fingerprints, and provider identifiers.
- The three delivered external emails are not reversible. No code, schema, environment, webhook, deployment, or production state changed in this step.

## Three-run unassisted inbound reply proof

### External event and backend evidence

- The owner replied separately in all three new AgentMail threads. This authorized verification only; no `Connected` continuation, new outbound, reply draft, provider configuration, or production change was authorized.
- Target: development `vibrant-wren-913`. Three new application inbound rows appeared after the replies. Together with the earlier tracer, all four event IDs, provider message IDs, and provider thread IDs are unique and all four events are processed.
- The earlier pre-fix event required a disclosed reconciliation roughly nine minutes after receipt. In contrast, the three new post-fix events were processed automatically about 17, 15, and 15 seconds after receipt. No reconciliation command or application mutation was run for these replies.
- Mail bindings remain exactly four with total `sendCount: 4`, real provider receipts on every row, zero recorded errors, and statuses of one `connected` plus three `replied`. Connections show the same one-plus-three status split, while continuation count remains exactly one.

### UI, persistence, and evidence boundary

- Reopening each of the three isolated Connections origins showed `They replied`, the matching provider thread reply, one approved outreach, and the separate `Yes, help me continue` decision. A second full navigation retained all three Replied states.
- The original three Contacted browser tabs had been reclaimed by the desktop browser before the owner replied, so this run did not visually observe the instant `Contacted → Replied` animation in those same tabs. The prior single tracer remains the direct no-refresh UI proof; this run proves automatic post-fix callbacks, unique event processing, session isolation, and reload persistence.
- No continuation button was pressed for any of the three. Each connection remains at the separate consent and state-semantics gate after `Replied`.

### Public-log and rollback boundary

- The public hackathon log records only redacted counts, timing ranges, and maturity. It omits mailbox addresses, reply text, subjects, private memories, payload hashes, and provider identifiers.
- The incoming emails and signed provider events are external facts and are not reversible. No code, schema, deployment, environment, webhook, outbound email, or production state changed in this verification step.

## Selective Connected continuations

### Authorization and execution

- The owner explicitly authorized only rehearsal runs 1/3 and 3/3 to advance from `Replied` to `Connected`, while requiring run 2/3 to remain `Replied`. This did not authorize any new email, external reply, commitment, or production change.
- Target: development `vibrant-wren-913`. The browser re-read all three run-specific subjects and confirmed that each connection was still `Replied` with its own `Yes, help me continue` action before changing state.
- The continuation action was used exactly once in run 1/3 and exactly once in run 3/3. Run 2/3's continuation control was not touched.

### Realtime, persistence, and exactly-once evidence

- The open screens changed without refresh: runs 1/3 and 3/3 displayed the restrained Connected completion state and removed their continuation controls, while run 2/3 remained `They replied` with its control still available.
- Redacted Convex reads found exactly three continuation rows for three unique connections: the earlier tracer plus the two newly authorized runs. Connections and mail bindings now contain three `connected` and one `replied` record.
- Mail bindings remain exactly four with total `sendCount: 4`, provider receipts on every row, and zero recorded errors. All four unique inbound events remain processed; no outbound or inbound count changed.
- Full navigation of all three isolated session URLs restored the same selective outcome: Connected for runs 1/3 and 3/3, Replied for run 2/3. No code, schema, environment, webhook, deployment, AgentMail message, or production state changed.

### Public evidence boundary

- The `/hackathon` update records only the authorization scope, redacted counts, visible state outcome, and reload proof. It omits contact details, message content, subjects, private memories, fingerprints, and provider identifiers.
