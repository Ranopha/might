# Worklog — 2026-09-03

## Authorization and scope

- The owner selected visual option 3: the living accordion-letter treatment
  for Connections and the matching tactile function-control language.
- Scope stayed inside visual quality and three-minute-demo clarity. No backend,
  provider, data, email, webhook, submission, or production-deploy action was
  authorized or performed.
- The accepted fixed-room hero and four-surface product boundary were retained.

## Shipped locally

- Added a generated transparent six-fold Connections accordion with a live
  semantic journey from `Noticed` through `Connected`.
- Added generated transparent paper assets for the central action tray and the
  curled consent reminder.
- Replaced the flat empty state with the selected paper-story composition and
  linked its real action to Might Found.
- Applied the existing generated primary/secondary paper plates to real Me,
  Might Found, Connections, Talk manifestation, Sound, and Settings actions.
- Replaced newly touched text-arrow and sparkle glyphs with Lucide icons.
- Preserved all consent, payload hash, AgentMail, reply, Connected, and Convex
  realtime behavior unchanged.

## Verification

- `npm run lint` — passed.
- `npm run typecheck` — passed.
- `npm test` — 7 files / 27 tests passed.
- `npm run build` — passed; Vite produced the static production bundle.
- Browser QA at 1490 × 1058 and 390 × 844 — passed.
  - Mobile measurement: `scrollWidth === innerWidth === 390`.
  - `See what Might found` navigated to `/found` and rendered the expected
    heading.
  - The paper Settings control opened the existing complete settings drawer.
  - Browser warning/error log after settling was empty.
- Combined reference-versus-implementation comparisons were inspected at full
  view and focused accordion detail. `design-qa.md` records `passed`.
- `git diff --check` — passed before the final documentation update.

## External effects

- Codex built-in image generation created original paper UI assets. Local alpha
  post-processing removed a solid key background; no API credential was used.
- No OpenAI application API, Firecrawl, AgentMail, email, webhook, Convex data,
  Convex deployment, or production static deployment was invoked.
- The unrelated untracked judging-research draft remained untouched.

## Rollback

- Revert the eventual single UI commit to restore the previous flat
  Connections state and controls. No database or provider rollback is needed.

## Next

- After owner review, obtain explicit approval for the exact commit before
  deploying its backend/static release to production `hushed-stork-401`.
- Once deployed, verify all six public routes plus desktop/mobile Connections;
  do not run a Sponsor call or send email as part of that visual release.

## Mobile Connections stacking correction

### Authorization and diagnosis

- The owner supplied a Safari capture showing the mobile Connections view with
  the accordion image, a detached lifecycle card, the story copy, and the
  persistent navigation visually stacked on one another.
- A repeatable browser geometry check reproduced the exact issue at 540 × 1058
  twice: the 246 px paper expanded into a 631 px journey, the stages and copy
  occupied separate vertical layers, and the floating navigation had only 78 px
  of reserved space for a 94 px dock envelope.
- Ranked probes confirmed both causes independently: placing the semantic stages
  back on the accordion removed the extra layer; moving the dock into its own
  app-shell row removed content/navigation overlap.

### Implementation

- Added one responsive accordion canvas so the generated paper and its six live,
  semantic stages remain one visual object from desktop through 320 px.
- Removed the mobile two-column and 420 px single-column stage-card expansion.
  Empty-state copy now follows the complete fold without a generic card between
  them.
- Changed the sub-980 px app shell into a `100dvh` main-scroll row plus a separate
  bottom-navigation row with safe-area margin. The nav remains persistent, but
  no longer sits over the story content.
- Kept the accepted room hero, generated assets, copy, four routes, consent
  behavior, and desktop composition unchanged.

### Verification

- The geometry check passed twice at 390 × 844 and 540 × 1058: all lifecycle
  labels stayed inside the paper, copy cleared it by 24 px, the navigation began
  exactly where the main viewport ended, and horizontal overflow was zero.
- 320 × 720 and the 980/981 breakpoint boundary passed. At maximum scroll, the
  action tray and consent note both cleared the navigation completely.
- A normalized 420 × 775 source-versus-final comparison and focused accordion
  comparison were opened together and inspected; `design-qa.md` records
  `final result: passed`.
- Talk, Me, Found, and Connections navigation passed in the revised shell. The
  Settings drawer opened/closed, the Connections CTA reached the expected Found
  heading, and the settled browser warning/error log was empty.
- `npm run lint`, `npm run typecheck`, `npm test` (7 files / 27 tests),
  `npm run build`, and `git diff --check` passed.
- The repository has no browser-layout test runtime at the required real layout
  seam, so the exact regression is retained as a repeatable in-app-browser
  geometry check rather than a misleading jsdom unit test.

### External effects and rollback

- No Convex function, schema, application data, environment, OpenAI, Firecrawl,
  AgentMail, email, webhook, or production deployment was changed or invoked.
- Local visual QA may have created only an empty browser-scoped development
  session through the existing app bootstrap path.
- Revert the eventual single mobile-layout commit to restore the prior responsive
  accordion and floating navigation; no backend or provider rollback is needed.

### Next

- Review this local mobile correction, then explicitly approve the exact commit
  before any production static deployment. No backend deploy is needed for this
  CSS/React-only change.

## Medium-desktop Connections RWD correction

### Authorization and diagnosis

- The owner supplied a second Safari capture showing that the empty
  Connections composition also collided at an approximately 1152 × 768 desktop
  viewport, then explicitly asked to replace brittle fixed placement with RWD.
- The real browser geometry loop reproduced the exact defect twice. With the
  252 px rail and screen padding, the paper received only 784.8125 px: the body
  paragraph crossed the lifecycle row and the privacy chip was covered by the
  fixed-overlap action tray.
- A one-variable typography probe cleared only the copy/stage collision. This
  confirmed two independent causes: viewport-sized inner type and a fixed tray
  overlap unrelated to available component width.

### Implementation

- Made both empty and populated paper journeys inline-size containers. Heading,
  body, stage labels, and privacy text now scale from the paper's own width.
- Coupled the tray overlap and privacy clearance to one component-relative
  spacing value, so the two paper layers cannot drift into one another.
- Added a component-width reflow at 740 px: the real accordion and all six
  stages stay together, while explanatory copy and privacy enter document flow
  below the paper. Existing 320–720 px mobile structure and navigation remain
  intact.

### Verification

- The original 1152 × 768 check passed twice after the fix: copy-to-stage gap
  20.7578 px, privacy-to-tray gap 8 px, and horizontal overflow zero.
- A 15-viewport geometry matrix passed at 320, 390, 420, 540, 720, 721, 800,
  980, 981, 1024, 1100, 1104, 1152, 1280, and 1440 px. It verifies paper-bound
  stages, the overlay/flow copy contract, privacy/tray clearance, dock
  separation, and horizontal containment.
- At 390 × 844 maximum scroll, the action and consent note cleared navigation by
  195.0781 px and 42.0781 px respectively. The four routes, Settings open/close,
  and the Connections CTA passed; the settled browser warning/error log was
  empty.
- The selected 1487 × 1058 source and final desktop capture were opened as one
  comparison, followed by a focused source-versus-1152 comparison. The accepted
  room, paper assets, three-line heading, six stages, and consent hierarchy were
  preserved; `design-qa.md` records `passed`.
- `npm run lint`, `npm run typecheck`, `npm test` (7 files / 27 tests),
  `npm run build`, and `git diff --check` passed. There is no real-browser test
  runtime at this layout seam, so the regression remains a repeatable in-app
  browser geometry check rather than a non-layout jsdom assertion.

### External effects and rollback

- No Convex function, schema, application data, environment, Sponsor API,
  email, webhook, or production deployment was changed or invoked. The existing
  unrelated untracked judging-research draft remained untouched.
- Revert `44c25e1` to restore the previous desktop sizing; no backend or
  provider rollback is required.

### Next

- Review the local 1152 px correction, then explicitly approve the exact commit
  before any production static deployment. No backend deploy is required.

## Connections balanced-footer correction

### Authorization and diagnosis

- The owner supplied a follow-up 1152 × 768 Safari capture and clarified the
  intended composition: `See what Might found` should sit to the left of
  `One step at a time`, with both pieces balancing one another instead of
  becoming two more vertical layers.
- A real-browser check reproduced the exact visual defect twice. The centered
  action tray crossed 100.125 px into the note's horizontal space, the boxes
  intersected, and their vertical centers differed by 77.25 px.
- The shared-layout-parent hypothesis ranked first and was confirmed: placing
  both surfaces in one grid removed all three failures. A 1024 px visual pass
  then found and corrected a two-line CTA label by changing only the tray's
  inner spacing to respond to component width.

### Implementation

- Added one semantic boundary-note component and placed the empty-state CTA and
  note inside a shared responsive footer. Desktop and sufficiently wide tablet
  layouts use a proportional left/right row; constrained layouts keep the same
  DOM order and switch to a clean non-overlapping stack.
- Removed independent negative placement from the empty-state action without
  changing the generated paper assets, accepted room, six-stage journey,
  consent wording, navigation, or any connection behavior.
- Kept the CTA label on one line with component-relative insets instead of a
  viewport-specific coordinate patch.

### Verification

- The original 1152 × 768 regression check passed twice: the action ends at
  860.8672 px, the note begins at 884.4063 px, their gap is 23.5391 px, both
  share center Y 1069.9453, and horizontal overflow is zero.
- A 14-width matrix passed at 320, 390, 420, 540, 720, 721, 800, 980, 981,
  1024, 1100, 1152, 1280, and 1440 px. It retained the earlier accordion,
  copy, privacy, bottom-navigation, and containment checks and added row/stack,
  footer containment, non-overlap, and CTA text-overflow assertions.
- The selected source and final implementation were opened as one full
  comparison and one focused accordion/footer comparison. The room and paper
  language are unchanged; the action and note now form the intended balanced
  footer. `design-qa.md` records `passed`.
- `See what Might found` still reached the expected Found heading; Settings
  opened and closed. The settled browser log contained no warning or error.
- `npm run lint`, `npm run typecheck`, `npm test -- --run` (7 files / 27
  tests), `npm run build`, and `git diff --check` passed.
- There is no real-browser test runtime at this layout seam, so the regression
  remains an in-app-browser geometry check rather than a misleading jsdom test.

### External effects and rollback

- No Convex function, schema, data, environment, Sponsor API, email, webhook,
  or production deployment was changed or invoked. The unrelated untracked
  judging-research draft remained untouched.
- Revert the eventual single frontend commit to restore the prior independent
  tray/note placement; no backend or provider rollback is required.

### Next

- Review the local balanced footer, then explicitly approve the exact commit
  before any production static deployment. No backend deploy is needed.
